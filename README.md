# MonoXtract

本仓库包含两个子项目：
- **DeepSIFA-automation**：含有DeepSIFA算法model，训练集以及测试集使用说明。可以用于训练模型。
- **vvv**：含TrackSIFA与DeepSIFA两部分的运行脚本与打包的可执行程序（“Boxsifa”可执行程序请到 *Releases* 下载）
（*Releases中包含所需的data文件夹）
## 安装依赖（示例）
```bash
pip install -r requirements.txt
